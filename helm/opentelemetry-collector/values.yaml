# OpenTelemetry Collector Configuration
# This values.yaml configures the collector to receive OTLP data from your services
# and export traces to Jaeger, metrics to Prometheus, and logs to Loki

mode: deployment

image:
  repository: otel/opentelemetry-collector-contrib

# Expose Prometheus metrics endpoint for scraping
service:
  type: ClusterIP

ports:
  otlp:
    enabled: true
    containerPort: 4317
    servicePort: 4317
    protocol: TCP
  otlp-http:
    enabled: true
    containerPort: 4318
    servicePort: 4318
    protocol: TCP
  prometheus:
    enabled: true
    containerPort: 8889
    servicePort: 8889
    protocol: TCP
  jaeger-grpc:
    enabled: false
  jaeger-thrift-http:
    enabled: false
  zipkin:
    enabled: false

# OpenTelemetry Collector configuration
config:
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: 0.0.0.0:4317
        http:
          endpoint: 0.0.0.0:4318

  processors:
    batch:
      timeout: 1s
      send_batch_size: 512

  exporters:
    # Export traces to Jaeger (all-in-one deployment)
    otlp/jaeger:
      endpoint: jaeger.observability.svc.cluster.local:4317
      tls:
        insecure: true
    
    # Export logs to Loki
    otlphttp/loki:
      endpoint: http://loki.observability.svc.cluster.local:3100/otlp
    
    # Export metrics to Prometheus (expose endpoint for scraping)
    prometheus:
      endpoint: 0.0.0.0:8889
      # namespace: ixp  # Removed to avoid double ixp_ prefix since instruments already have ixp. prefix
      const_labels:
        environment: development
    
    # Debug exporter for troubleshooting - outputs all signals (logs, metrics, traces) to stdout
    # This ensures you can see real-time logs via kubectl logs
    debug:
      verbosity: detailed

  extensions:
    health_check:
      endpoint: 0.0.0.0:13133

  service:
    extensions: [health_check]
    pipelines:
      traces:
        receivers: [otlp]
        processors: [batch]
        exporters: [otlp/jaeger, debug]
      logs:
        receivers: [otlp]
        processors: [batch]
        exporters: [otlphttp/loki, debug]
      metrics:
        receivers: [otlp]
        processors: [batch]
        exporters: [prometheus, debug]

# ServiceMonitor to register OTEL collector metrics with Prometheus operator
# This tells Prometheus to scrape metrics from the OTEL collector's prometheus exporter (:8889)
serviceMonitor:
  enabled: true
  namespace: observability
  labels:
    release: observability
  interval: 10s
  scrapeTimeout: 10s
  relabelings: []
  endpoints:
    - port: prometheus
      interval: 30s